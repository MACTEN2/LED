# Copy this file to .env and fill in real values. .env is gitignored.

# SQLite is the local file at db/db.db by default — leave blank unless you
# need to point at a different file (e.g. a separate test database).
SQLITE_PATH=

# Random long string used to sign the session cookie
SESSION_SECRET=
